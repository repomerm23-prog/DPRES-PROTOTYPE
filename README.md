
  # Disaster Preparedness Platform Design

  This is a code bundle for Disaster Preparedness Platform Design. The original design resources were created as part of a Design Kit for this project.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  